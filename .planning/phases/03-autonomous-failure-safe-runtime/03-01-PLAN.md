---
phase: 03-autonomous-failure-safe-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - holidays.py
  - notify.py
  - sentinel.py
  - tests/test_holidays.py
  - tests/test_notify.py
  - tests/test_sentinel.py
autonomous: true
requirements: [RUN-02, NOTIFY-05]
must_haves:
  truths:
    - "Running sentinel on an NSE trading holiday exits cleanly (return 0) with no broker auth, no price fetch, and no Telegram digest (D-03)."
    - "Running sentinel on a Saturday or Sunday exits cleanly (return 0) without a digest (D-03)."
    - "Every clean 'ran fine' exit path (successful send, dry-run print, holiday/weekend skip, no-holdings) pings HEALTHCHECK_URL when it is set; the auth/fetch error path never pings (D-08)."
    - "A run dated past the last seeded holiday year emits a loud warning (stderr + best-effort Telegram) instead of silently assuming the market is open (D-03)."
  artifacts:
    - "holidays.py: NSE_HOLIDAYS_2026 (15 dates), LAST_SEEDED_YEAR, is_trading_holiday(today)->(bool, warning|None)"
    - "notify.healthcheck_ping(url) best-effort GET heartbeat helper"
    - "sentinel._market_closed(today) pure injectable helper + main() holiday/weekend early-exit and ping wiring"
    - "tests/test_holidays.py, extended tests/test_notify.py, extended tests/test_sentinel.py"
  key_links:
    - "sentinel.main computes IST today then calls _market_closed(today) BEFORE broker.get_client — no network before the skip decision"
    - "healthcheck ping sits on clean-exit paths only, never inside the except handler (a real failure must stay unpinged so the dead-man's-switch trips)"
---

<objective>
Ship the Python half of autonomous runtime: a static NSE holiday skip and an
independent dead-man's-switch heartbeat, both unit-testable with an injected
`today` and mocked ping — no CI required to prove them.

Add `holidays.py` (static 2026 NSE trading-holiday set + `is_trading_holiday`,
loud-warn past the last seeded year per D-03), a best-effort
`notify.healthcheck_ping(url)` heartbeat, and wire `sentinel.main()` to
early-exit cleanly on weekends/holidays (per D-03) and to ping healthchecks.io
on every "ran fine" exit but never on the error path (per D-08).

Purpose: A holiday must not fire a false digest, and a missed cron or a
crash-before-send must be detectable rather than mistaken for "all quiet".
Output: `holidays.py`, `notify.healthcheck_ping`, `sentinel._market_closed` +
main() wiring, and their tests.
</objective>

## Phase Goal

**As a** the sole operator of Groww Sentinel, **I want to** have the digest run
itself every weekday and loudly signal any failure or missed run, **so that** I
never have to trigger it manually and never mistake silence for "all quiet".

*(Derived from the ROADMAP Phase 3 goal, which is prose rather than strict
user-story form. Phase 3 is CI/runtime glue, so the MVP vertical slice is
naturally: this plan = the testable Python behavior; 03-02 = the GitHub Actions
workflow that runs it. Each code-producing task writes its tests first.)*

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-autonomous-failure-safe-runtime/03-CONTEXT.md
@.planning/phases/03-autonomous-failure-safe-runtime/03-RESEARCH.md

# Entrypoint being modified + the outbound-HTTP module gaining the heartbeat
@sentinel.py
@notify.py

# Test patterns to mirror (monkeypatch broker/prices/state, AAA structure)
@tests/test_sentinel.py
@tests/test_notify.py
</context>

<interface_context>
`sentinel.main(argv)` (in `sentinel.py`) already:
- validates 4 required secrets, returns 2 if any missing (never reaches the try block)
- inside a try: computes `today = datetime.now(ZoneInfo("Asia/Kolkata")).date()`,
  loads state, `broker.get_client(...)`, `broker.get_holdings(client)`; returns 0
  early with a best-effort notify when there are no holdings
- on the `--dry-run` flag prints the digest and returns 0; otherwise `notify.send(...)` then returns 0
- on any exception: redacts, prints to stderr, best-effort Telegram warning, returns 1

`notify.send(token, chat_id, text)` is the existing thin `requests.post` to Telegram;
`requests` is already imported in `notify.py`. `_best_effort_notify(env, message)` and
`_redact(text, env)` already exist in `sentinel.py`.

Optional env vars this plan reads: `HEALTHCHECK_URL` (may be unset — helper no-ops).
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: holidays.py static NSE 2026 holiday set + is_trading_holiday (RUN-02, D-03)</name>
  <files>holidays.py, tests/test_holidays.py</files>
  <read_first>
    - 03-RESEARCH.md → Architecture Patterns → Pattern 1 (the authoritative 15-date 2026 list + LAST_SEEDED_YEAR + is_trading_holiday signature and warn-path rationale)
    - 03-RESEARCH.md → Code Examples → "Minimal test shape for holidays.py" (AAA test names to mirror)
    - tests/test_state.py for the repo's stdlib-only pure-module test style
  </read_first>
  <behavior>
    - is_trading_holiday(date(2026, 1, 26)) -> (True, None)  # Republic Day
    - is_trading_holiday(date(2026, 1, 27)) -> (False, None) # ordinary weekday
    - is_trading_holiday(date(2026, 12, 25)) -> (True, None) # Christmas (last seeded date)
    - is_trading_holiday(date(2028, 1, 1)) -> (False, warning) where warning is a non-empty string containing "2028"
    - The module holds exactly the 15 NSE 2026 dates enumerated in 03-RESEARCH Pattern 1; weekends are NOT in this set (they are sentinel's separate concern).
  </behavior>
  <action>
    Create `holidays.py` as a pure stdlib data module (no new dependency — this is
    the whole point of D-03 choosing a static list over `pandas_market_calendars`).
    Seed a module-level frozen set `NSE_HOLIDAYS_2026` with the 15 `datetime.date`
    literals enumerated in 03-RESEARCH.md Pattern 1 (2026-01-15 Maharashtra elections
    through 2026-12-25 Christmas), each with the same trailing-comment holiday name.
    Define `LAST_SEEDED_YEAR = 2026` and `ALL_HOLIDAYS = NSE_HOLIDAYS_2026` (a union
    point for future years). Expose `is_trading_holiday(today: date) -> tuple[bool, str | None]`
    returning `(today in ALL_HOLIDAYS, warning)` where `warning` is set to a loud,
    human-readable string naming `today.year` and `LAST_SEEDED_YEAR` ONLY when
    `today.year > LAST_SEEDED_YEAR` (fail-loud per D-03: never silently assume "open"),
    otherwise None. Do NOT include weekend logic here — Saturdays/Sundays are handled
    by sentinel via `today.weekday()`. Add a short module note that 2027 dates are not
    yet published by NSE (per D-03/03-RESEARCH Assumptions Log A2) and are added when
    published, with the warn-path covering the gap until then.
    Write `tests/test_holidays.py` first (AAA, pytest, mirroring the behavior block),
    confirm RED, then implement to GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_holidays.py -q && grep -c 'date(2026' holidays.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_holidays.py` passes (all behavior-block cases green).
    - `grep -c 'date(2026' holidays.py` returns 15 (exactly the 15 seeded 2026 dates).
    - `grep -c 'date(2026, 1, 26)' holidays.py` >= 1 (Republic Day present).
    - `is_trading_holiday(date(2028,1,1))` returns a warning string containing "2028"; no exception raised.
    - holidays.py imports only from the stdlib `datetime` (no third-party import).
  </acceptance_criteria>
  <done>holidays.py exists with 15 seeded 2026 dates and a fail-loud past-year warning; tests/test_holidays.py is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: healthcheck heartbeat + sentinel holiday/weekend early-exit wiring (NOTIFY-05/D-08, RUN-02/D-03)</name>
  <files>notify.py, sentinel.py, tests/test_notify.py, tests/test_sentinel.py</files>
  <read_first>
    - 03-RESEARCH.md → Pattern 2 (healthchecks.io ping as a best-effort side effect that must never fail the run) and Pattern 2's illustrative sentinel call sites
    - 03-CONTEXT.md → D-08 (ping only on true-success + clean holiday/no-holdings exits, never on error) and D-03 (holiday/weekend → clean early exit, no digest)
    - sentinel.py existing `_best_effort_notify` / `_redact` for the best-effort try/except style to copy
    - tests/test_sentinel.py existing `test_main_wires_...` for the monkeypatch pattern (broker/prices/state stubbed, no live calls)
  </read_first>
  <behavior>
    - notify.healthcheck_ping(None) is a no-op (returns without any HTTP call).
    - notify.healthcheck_ping("https://hc-ping.com/uuid") issues a GET to that URL with a timeout; a raised requests exception is swallowed (function still returns None, never propagates).
    - sentinel._market_closed(date(2026, 1, 26)) is True (holiday); _market_closed(date(2026, 7, 11)) is True (Saturday); _market_closed(date(2026, 7, 10)) is False (ordinary Friday).
    - main() on a closed day: returns 0, does NOT call broker.get_client, and calls notify.healthcheck_ping(HEALTHCHECK_URL).
    - main() on a normal successful run: calls notify.healthcheck_ping exactly on the clean exit (after send / after dry-run print).
    - main() when broker/prices raise: returns 1 and does NOT call notify.healthcheck_ping.
    - main() on a date past LAST_SEEDED_YEAR: surfaces the warning (stderr) and still proceeds (does not early-exit on the warning alone).
  </behavior>
  <action>
    In `notify.py`, add `healthcheck_ping(url: str | None) -> None`: return immediately
    if `url` is falsy; otherwise `requests.get(url, timeout=10)` wrapped in a bare
    best-effort `try/except Exception: pass` — a monitoring heartbeat must never crash
    or fail the run (D-08). Do not construct any message from the exception (keeps the
    URL out of any surfaced text, per 03-RESEARCH Security Domain). Keep notify.py under
    the project's ~200-line module guideline.

    In `sentinel.py`, `import holidays` and add a small pure helper
    `_market_closed(today) -> bool` returning `today.weekday() >= 5 or holidays.is_trading_holiday(today)[0]`
    (injectable `today` so it is unit-testable without patching the clock — this is the
    Claude's-Discretion "prefer Python, testable" choice from 03-CONTEXT). In `main()`,
    immediately after computing IST `today` and BEFORE `broker.get_client`, unpack
    `is_holiday, warning = holidays.is_trading_holiday(today)`; if `warning` is set, print
    it to stderr and `_best_effort_notify(env, warning)` (loud per D-03) but continue.
    Then `if _market_closed(today): notify.healthcheck_ping(env.get("HEALTHCHECK_URL"))`,
    print a short "market closed — skipping" line, and `return 0` (no auth, no fetch, no
    digest). Add `notify.healthcheck_ping(env.get("HEALTHCHECK_URL"))` on the three
    remaining clean-exit paths — the existing no-holdings `return 0`, the `--dry-run`
    print `return 0`, and the real `notify.send(...)` `return 0` — and add it to NONE of
    the error returns (missing-secret return 2, and the `except` return 1). Keep sentinel.py
    focused; prefer terse comments so it stays near the ~200-line module guideline.

    Extend `tests/test_notify.py` (monkeypatch `notify.requests.get`) and
    `tests/test_sentinel.py` (mirror the existing monkeypatch-everything pattern; make
    `broker.get_client` a raiser to prove it is never reached on a closed day; record
    healthcheck_ping calls via a monkeypatched stub). Write the tests first, confirm RED,
    implement to GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_sentinel.py tests/test_notify.py -q && test $(grep -c 'healthcheck_ping' sentinel.py) -ge 4</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_notify.py` and `tests/test_sentinel.py` pass, including: closed-day early-exit returns 0 without calling `broker.get_client`; ping fires on success + closed-day + no-holdings + dry-run; ping does NOT fire on the exception path.
    - `notify.healthcheck_ping(None)` performs no HTTP call; a raised `requests` error inside `healthcheck_ping` is swallowed.
    - `grep -c 'healthcheck_ping' sentinel.py` >= 4 (holiday/weekend exit + no-holdings + dry-run + real send).
    - `_market_closed(today)` is True for a seeded 2026 holiday and for a Saturday, False for an ordinary weekday.
    - The full suite still passes: `python -m pytest -q` (Phase 1/2 tests remain green — no live network/broker calls).
  </acceptance_criteria>
  <done>Closed days early-exit(0) with a heartbeat and no network; the heartbeat fires on all clean exits and never on the error path; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sentinel process → healthchecks.io | Outbound heartbeat GET carrying the (secret) HEALTHCHECK_URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | `notify.healthcheck_ping` | low | mitigate | Bare best-effort `try/except: pass`; never builds a message from the exception, so the URL cannot leak into a log or Telegram string (03-RESEARCH Security Domain). |
| T-03-05 | Denial of Service (self-inflicted) | ping / holiday early-exit on the run path | medium | mitigate | Both are wrapped so they can never raise: a monitor or a stale holiday list must not become a second point of failure that breaks an otherwise-good run (D-08; D-03 warn-not-crash). |
</threat_model>

<verification>
- `python -m pytest -q` — entire suite green (Phase 1/2 + new holiday/ping tests), zero live network/broker/yfinance calls.
- Manual sanity (optional, local): `HEALTHCHECK_URL= python -m sentinel --dry-run` on a known holiday date path exits 0 and prints the "market closed" line (verified via the injected-`today` unit tests rather than the real clock).
</verification>

<success_criteria>
- A holiday or weekend run is a clean no-digest exit(0); a real auth/fetch failure still exits non-zero and stays unpinged.
- healthchecks.io receives a heartbeat on every "ran fine" outcome and only those.
- holidays.py is a dependency-free static module a human can extend by pasting next year's dates; a run past 2026 warns loudly.
</success_criteria>

<output>
Create `.planning/phases/03-autonomous-failure-safe-runtime/03-01-SUMMARY.md` when done.
</output>
