---
phase: 01-end-to-end-morning-digest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - rules.py
  - tests/test_rules.py
autonomous: true
requirements: [RULES-01, RULES-02, RULES-03, RULES-04, RULES-05, STATE-05, TEST-01]
must_haves:
  truths:
    - "Given (holdings, config, state, today), evaluate() returns exactly one flag per held stock (RULES-02)."
    - "A stock qualifying for several flags resolves via the ordered precedence chain to the single expected flag (D-13)."
    - "A stock exactly at a threshold does NOT fire; just past it fires — strict > everywhere including AVG tiers (D-14)."
    - "Every AVG CANDIDATE result carries its tranche tier and marks the 3-gate reminder as coupled (RULES-05, D-03)."
    - "A symbol missing from config, or tagged anything other than core/tactical, resolves to UNTAGGED and never hard-fails (RULES-04, D-11)."
    - "The weight<10% gate suppresses AVG CANDIDATE at every tier, including -30% (D-04); weight denominator is total equity holdings value (D-02)."
  artifacts:
    - requirements.txt
    - rules.py
    - tests/test_rules.py
  key_links:
    - "rules.py imports only stdlib (datetime) — no network, file, clock, or env access (RULES-01)."
    - "All thresholds are named constants at the top of rules.py, one place to tune (RULES-03, D-01)."
    - "state is treated as a read-only lookup; in Phase 1 it is always {} so every symbol is first-seen (STATE-05)."
---

<objective>
Build the pure decision core `rules.py` and its full boundary test suite. This is the
money path: a side-effect-free `evaluate(holdings, config, state, today)` that maps each
holding to exactly one flag via a single ordered precedence resolver, using named
threshold constants and a first-run in-memory peak seed.

Purpose: Get the highest-value, highest-risk logic correct and fully tested before any
I/O module exists, per the locked TDD-first build order.
Output: `requirements.txt` (pinned deps), `rules.py`, `tests/test_rules.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-end-to-end-morning-digest/01-CONTEXT.md
@.planning/phases/01-end-to-end-morning-digest/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-end-to-end-morning-digest/SKELETON.md
</context>

## Phase Goal

**As a** Groww equity investor, **I want to** trigger one run that fetches my real holdings and live prices, evaluates my profit-booking/averaging strategy, and sends a per-stock flag digest to Telegram, **so that** each morning I know which holdings need attention without opening the app.

> Derived from the approved Core Value in PROJECT.md (the ROADMAP `**Goal:**` line states the same capability in outcome form). This plan delivers the strategy-evaluation core of that story.

## MVP Slice Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin dependencies and write the failing rules boundary test suite (RED)</name>
  <read_first>
    - .planning/phases/01-end-to-end-morning-digest/01-RESEARCH.md — "Pattern 4" (evaluate signature + fixture shape), "Common Pitfalls" 1 & 2, "Code Examples" (rules.py fixtures, AAA), Assumptions A1/A2/A3.
    - .planning/phases/01-end-to-end-morning-digest/01-CONTEXT.md — D-01, D-02, D-03, D-04, D-13, D-14.
    - .planning/research/STACK.md — pinned versions (growwapi 1.5.0, pyotp 2.10.0, requests 2.32.3, PyYAML 6.0.3, pytest 8.4.2).
  </read_first>
  <files>requirements.txt, tests/test_rules.py</files>
  <behavior>
    Write tests FIRST (they must fail — rules.py does not exist yet). Cover, AAA-structured:
    - STOP HIT (tactical): drop exactly 12.00% from avg cost → NOT STOP HIT (D-14); drop 12.01% → STOP HIT.
    - TRIM (any bucket): weight exactly 10.00% → NOT TRIM; 10.01% → TRIM. Weight denominator = sum of qty*ltp across all held symbols (D-02).
    - BOOK 50% (tactical): gain exactly 25.00% → NOT BOOK 50%; 25.01% → BOOK 50%.
    - TRAIL WATCH (core): exactly 20.00% below peak → NOT TRAIL WATCH; 20.01% → TRAIL WATCH.
    - AVG CANDIDATE (core): drop exactly 10.00% → no tier; 10.01% → tier 1; 20.01% → tier 2; 30.01% → tier 3. Message field reports the tranche.
    - AVG weight gate (D-04): a core stock down 31% but at weight 10.01% resolves to TRIM (weight gate suppresses AVG at every tier).
    - Precedence (D-13), first match wins: UNTAGGED > STOP HIT > TRIM > BOOK 50% > TRAIL WATCH > AVG CANDIDATE (deepest tier) > HOLD. Include one stock qualifying simultaneously for STOP HIT and TRIM asserting STOP HIT wins; one qualifying for TRIM and BOOK 50% asserting TRIM wins.
    - UNTAGGED (RULES-04, D-11): symbol absent from config → UNTAGGED; symbol tagged "bogus-typo" → UNTAGGED; run does not raise.
    - AVG reminder coupling (RULES-05): every AVG CANDIDATE flag entry carries a truthy marker/field the digest layer uses to attach the 3-gate reminder.
    - HOLD: a tagged stock triggering nothing → HOLD.
    - STATE-05 / Pitfall 1: with state={}, peak is seeded same-run as max(ltp, avg_cost); a below-peak clause therefore reports a same-run-seeded value (assert it behaves as first-seen, do NOT attempt multi-day drawdown — that is Phase 2). Add a docstring/comment in the test naming this expected Phase-1 behavior.
    - ltp is None for a symbol → an explicit "no price" result surfaced (not silently HOLD), run does not raise.
    Use the exact input dict shape {symbol, qty, avg_cost, ltp} and call rules.evaluate([...], config, state={}, today=date(2026,7,9)).
  </behavior>
  <action>
    Create `requirements.txt` pinning exactly: growwapi==1.5.0, pyotp==2.10.0, requests==2.32.3, PyYAML==6.0.3, pytest==8.4.2 (per STACK.md; these are the phase's full dependency set, consumed unchanged by later plans). Do NOT add pandas_market_calendars or any Phase 3 dependency.
    Create `tests/test_rules.py` implementing the boundary matrix in the behavior block, AAA-structured, using a small `holding(...)` fixture helper. Encode the strict-`>` boundary pairs (exactly-at = no-fire, just-past = fire) per D-14, and the multi-qualifying precedence cases per D-13. Do NOT write rules.py in this task — the suite must fail because the import target is missing.
    Verify pip can install the pinned set (`pip install -r requirements.txt`) before running the suite.
  </action>
  <verify>
    <automated>pip install -r requirements.txt >/dev/null 2>&1 && python -m pytest tests/test_rules.py -q 2>&1 | grep -Eq "error|failed|no tests ran|ModuleNotFoundError|collected [1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt lists all 5 pinned packages and nothing from a later phase.
    - tests/test_rules.py collects a non-trivial number of tests and they FAIL/ERROR (rules.py absent) — proving RED before GREEN.
    - The suite includes strict-boundary pairs for STOP HIT, TRIM, BOOK 50%, TRAIL WATCH, and AVG tiers; at least two multi-qualifying precedence assertions; an UNTAGGED case for both missing and bad-tag; and an AVG-reminder-coupling assertion.
  </acceptance_criteria>
  <done>requirements.txt is pinned and installable; tests/test_rules.py collects the full boundary/precedence/UNTAGGED/reminder matrix and fails (RED) because rules.py does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pure rules.py to green (GREEN + REFACTOR)</name>
  <read_first>
    - tests/test_rules.py (from Task 1) — the contract this task must satisfy.
    - .planning/phases/01-end-to-end-morning-digest/01-RESEARCH.md — "Pattern 4", "Architectural Responsibility Map", "Anti-Patterns to Avoid" (no clock/env inside rules).
    - .planning/research/ARCHITECTURE.md — "Pattern 1: Pure-core/imperative-shell", threshold constant example.
  </read_first>
  <files>rules.py</files>
  <behavior>
    Implement `evaluate(holdings, config, state, today)` so every Task-1 test passes.
    - Named constants at module top (RULES-03, D-01): AVG drop tiers 0.10/0.20/0.30; AVG weight gate 0.10; TRIM weight 0.10; BOOK gain 0.25; STOP drop 0.12; STOP below-peak 0.15; TRAIL below-peak 0.20.
    - Per stock: pnl fraction vs avg cost, weight fraction (denominator = sum of qty*ltp across held symbols, D-02), peak = max(ltp, avg_cost) when state has no prior peak (STATE-05, A3), pct_below_peak.
    - Single ordered resolver (NOT independent ifs), first match wins (D-13): UNTAGGED → STOP HIT (tactical: drop>0.12 or below-peak>0.15) → TRIM (any: weight>0.10) → BOOK 50% (tactical: gain>0.25) → TRAIL WATCH (core: below-peak>0.20) → AVG CANDIDATE (core: drop>0.10, weight<0.10, deepest tier of 0.30/0.20/0.10) → HOLD.
    - All comparisons strict `>` / `<` per D-14.
    - AVG CANDIDATE result includes its tier number and a coupled 3-gate reminder marker (RULES-05); ltp None yields an explicit no-price result.
    - Returns (flags, new_state); new_state rebuilt from current holdings only (read old state as lookup). Module docstring names the Phase-1 "state is always {} / below-peak clause is same-run-seeded" fact (Pitfall 1).
  </behavior>
  <action>
    Implement `rules.py` per the behavior block until `tests/test_rules.py` is fully green. Keep the file under 200 lines. Import only `datetime` from stdlib — no network, no file, no `date.today()`, no `os.environ` (Anti-Pattern 1). Implement flag resolution as one ordered chain returning on first match (D-13), never a set of independent `if` branches. Cite the decisions in code comments where they drive a constant or ordering (D-01 constants, D-02 weight denominator, D-04 gate-all-tiers, D-13 precedence, D-14 strict operators). Do not introduce config/threshold-override plumbing (D-10 deferred).
  </action>
  <verify>
    <automated>python -m pytest tests/test_rules.py -q && grep -Eq "def evaluate" rules.py && [ "$(grep -vE '^\s*#' rules.py | grep -c 'date.today()')" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_rules.py` passes with zero failures.
    - `rules.py` defines `evaluate` and named threshold constants at module top; contains no `date.today()` / `os.environ` / network / file access.
    - Flag resolution is a single ordered chain (first-match-wins), verifiable by the passing multi-qualifying precedence tests.
    - File is under 200 lines.
  </acceptance_criteria>
  <done>rules.py implements the pure ordered-resolver evaluate() with named constants and first-run peak seed; tests/test_rules.py is fully green (TEST-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → dev env | `pip install -r requirements.txt` pulls third-party code |
| config data → rules core | `config` dict values are untrusted user input (may be typo/missing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pip installs (growwapi/pyotp/requests/PyYAML/pytest) | high | accept | RESEARCH.md Package Legitimacy Audit verdicts all OK/Approved (pyotp, PyYAML, pytest, requests long-established; growwapi is official Groww vendor SDK, sole sanctioned path). No [ASSUMED]/[SUS]/[SLOP] packages → no legitimacy checkpoint required. Pin exact versions in requirements.txt. |
| T-01-01 | Denial of Service | rules.evaluate on malformed holding/config | low | mitigate | Bad/missing config tag resolves to UNTAGGED, never raises (D-11); ltp None yields explicit no-price result, never a KeyError/TypeError propagating out. |
</threat_model>

<verification>
- `pip install -r requirements.txt` succeeds with the pinned set.
- `python -m pytest tests/test_rules.py -q` is all-green after Task 2.
- Boundary pairs (exactly-at vs just-past) confirm strict `>` (D-14); multi-qualifying tests confirm the precedence chain (D-13).
- `rules.py` is pure (no clock/env/network/file) and under 200 lines.
</verification>

<success_criteria>
`rules.py` is a pure, no-I/O `evaluate(holdings, config, state, today)` with named threshold
constants, a single ordered precedence resolver producing exactly one flag per stock, the AVG
3-gate reminder coupling, UNTAGGED tolerance, and a first-run in-memory peak seed — with a
fully passing boundary unit suite (TEST-01).
</success_criteria>

## Artifacts this plan produces

- `requirements.txt` — pinned: `growwapi==1.5.0`, `pyotp==2.10.0`, `requests==2.32.3`, `PyYAML==6.0.3`, `pytest==8.4.2`.
- `rules.py` — function `evaluate(holdings, config, state, today) -> (flags, new_state)`; threshold constants (AVG drop tiers, AVG weight gate, TRIM weight, BOOK gain, STOP drop, STOP below-peak, TRAIL below-peak); ordered precedence resolver; flag values `UNTAGGED`, `STOP HIT`, `TRIM`, `BOOK 50%`, `TRAIL WATCH`, `AVG CANDIDATE` (with tier), `HOLD`, plus an explicit no-price result; per-flag entry fields consumed by notify (symbol, flag, bucket, pct, tier, weight, reminder marker).
- `tests/test_rules.py` — AAA boundary matrix + precedence + UNTAGGED + reminder-coupling tests.

<output>
Create `.planning/phases/01-end-to-end-morning-digest/01-01-SUMMARY.md` when done.
</output>
